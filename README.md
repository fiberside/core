The Thing
=========

The Thing is minimalistic CMF, that extends Yii and also offers numerous components to simplify the development of web-applications.

Development
-----------

All code changes are performed only in the develop-branch, because the master-branch contains the previous stable version of the project. Also the project uses development plan like as git-flow.